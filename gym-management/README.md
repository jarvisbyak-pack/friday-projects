# Gym Management System

## Project Structure
- `/client`: React application (Tailwind CSS)
- `/server`: Node.js/Express application
- `/database`: SQL schema files

## Next Steps
1. Setup client-side structure (Vite + React + Tailwind)
2. Setup server-side structure (Express + Node)
3. Define database schema (PostgreSQL)
